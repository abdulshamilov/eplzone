import { StubPage } from "@/components/stub-page";

export const metadata = { title: "Вход — EPL Zone" };

export default function LoginPage() {
  return (
    <StubPage
      title="Вход"
      note="Форма входа появится после подключения JWT-аутентификации на бэкенде."
    />
  );
}
